import { InfiniteWorkspace } from "@/components/landing/infinite-workspace";

export const metadata = {
  title: "Keeps — Infinite Workspace",
  description: "Explore a desk of notes, entries, and emerging patterns.",
};

export default function Homepage3Page() {
  return <InfiniteWorkspace />;
}
