/** Minimum sealed entries before the server runs pattern generation. */
export const PATTERN_GENERATION_MIN_SEALED_ENTRIES = 5;

/** Minimum total words across sealed entries before pattern generation. */
export const PATTERN_GENERATION_MIN_TOTAL_WORDS = 500;
