/** Canvas snapshot constants — server-safe (no "use client" dependencies). */

export const CANVAS_SNAPSHOT_VERSION = 4 as const;

/** Page + canvas background token (`global.css`). */
export const CANVAS_BACKGROUND = "var(--canvas-bg-gradient)";
